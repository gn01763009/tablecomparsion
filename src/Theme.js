import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      main: '#2A865C',
    },
    secondary: {
      main: '#6052FF',
    },
    other: {
      white: 'white',
      black: '#00000'
    },
  },
});

export default Theme
